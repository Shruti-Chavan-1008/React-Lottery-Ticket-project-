LuckyDraw is a simple lottery ticket game built using React.js. 
Users can generate random tickets, and the application checks whether 
the generated ticket matches the winning criteria. It provides an 
interactive UI while demonstrating core React concepts like useState, 
components, and props.